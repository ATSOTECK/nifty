package random

fn randomRangei(to, from: int): int {
    //
}

fn randomRangef(to, from: float): float {
    //
}

fn randomRanged(to, from: double): double {
    //
}

fn randomRange overloads{randomRangei, randomRangef, randomRanged}

#[min_arity(2)]
fn choose<T>(items: ..T): T {
    choice := randomRangei(0, len(items))
    #noBoundsCheck {
        return items[choice]
    }
}