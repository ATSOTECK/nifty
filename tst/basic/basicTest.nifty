package basic;

fn add(x: int y: int): int {
    return x + y;
}

/-
fn add2(x, y: int): int {
    return x + y;
}

fn add3(x := 10, y := 10) {
    return x + y;
}
-/

fn main(): int {
    return add(2, 2);
}
